import React, { memo } from 'react'

import DetailInfosWrapper from './style'

const DetailInfos = memo(() => {
  return <DetailInfosWrapper>DetailInfos</DetailInfosWrapper>
})

export default DetailInfos
